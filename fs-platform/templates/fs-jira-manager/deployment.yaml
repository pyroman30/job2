{{ if or (not (hasKey $.Values.fs_jira_manager "enabled")) (eq (coalesce $.Values.fs_jira_manager.enabled false) true) }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fs-jira-manager
  namespace: {{ $.Values.settings.namespace }}
  labels:
    app: fs-jira-manager
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: fs-jira-manager
  template:
    metadata:
      annotations:
      {{- if $.Values.fs_jira_manager.env }}
        checksum/configmap: {{ include (print $.Template.BasePath "/fs-jira-manager/configmap.yaml") . | sha256sum | quote }}
      {{- end }}
      
      labels:
        app: fs-jira-manager
    spec:
      containers:
      - name: {{ .Chart.Name }}
        image: {{ $.Values.fs_jira_manager.image.repository }}:{{ $.Values.fs_jira_manager.image.tag }}
        imagePullPolicy: {{ .Values.fs_jira_manager.image.pullPolicy }}
        ports:
        - containerPort: 80
        envFrom:
        {{- if $.Values.fs_jira_manager.env }}
        - configMapRef:
            name: fs-jira-manager-config
        {{- end }}
        
        - secretRef:
            name: fs-jira-manager-secret
        
        resources:
          requests:
            {{- toYaml $.Values.fs_jira_manager.requests | nindent 12 }}
          limits:
            {{- toYaml $.Values.fs_jira_manager.limits | nindent 12 }}
        {{- if $.Values.fs_jira_manager.livenessProbe }}
        livenessProbe:
          httpGet:
            path: /healthcheck
            port: 8000
            {{- toYaml $.Values.fs_jira_manager.livenessProbe | nindent 10 }}
        {{- end }}
        {{- if $.Values.fs_jira_manager.readinessProbe }}
        readinessProbe:
          httpGet:
            path: /healthcheck
            port: 8000
            {{- toYaml $.Values.fs_jira_manager.readinessProbe | nindent 10 }}
        {{- end }}
{{- end }}

