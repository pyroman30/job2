{{ if or (not (hasKey $.Values.fs_jira_manager "enabled")) (eq (coalesce $.Values.fs_jira_manager.enabled false) true) }}
kind: Service
apiVersion: v1
metadata:
  name: fs-jira-manager
  labels:
    app: fs-jira-manager
spec:
  type: {{ $.Values.fs_jira_manager.service.type | default "ClusterIP" | quote }}
  ports:
  - name: http
    protocol: TCP
    port: {{ $.Values.fs_jira_manager.service.port }}
    targetPort: {{ $.Values.fs_jira_manager.service.targetport }}
  selector:
    app: fs-jira-manager
{{- end }}