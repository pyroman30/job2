{{ if or (not (hasKey $.Values.fs_file_manager "enabled")) (eq (coalesce $.Values.fs_file_manager.enabled false) true) }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fs-file-manager
  namespace: {{ $.Values.settings.namespace }}
  labels:
    app: fs-file-manager
spec:
  replicas: {{ .Values.fs_file_manager.replicaCount }}
  selector:
    matchLabels:
      app: fs-file-manager
  template:
    metadata:
      annotations:
      {{- if $.Values.fs_file_manager.env }}
        checksum/configmap: {{ include (print $.Template.BasePath "/fs-file-manager/configmap.yaml") . | sha256sum | quote }}
      {{- end }}
      
      labels:
        app: fs-file-manager
    spec:
      containers:
      - name: {{ .Chart.Name }}
        image: {{ $.Values.fs_file_manager.image.repository }}:{{ $.Values.fs_file_manager.image.tag }}
        imagePullPolicy: {{ .Values.fs_file_manager.image.pullPolicy }}
        ports:
        - containerPort: 80
        envFrom:
        {{- if $.Values.fs_file_manager.env }}
        - configMapRef:
            name: fs-file-manager-config
        {{- end }}
        
        - secretRef:
            name: fs-file-manager-secret
        
        resources:
          requests:
            {{- toYaml $.Values.fs_file_manager.requests | nindent 12 }}
          limits:
            {{- toYaml $.Values.fs_file_manager.limits | nindent 12 }}
        {{- if $.Values.fs_file_manager.livenessProbe }}
        livenessProbe:
          httpGet:
            path: /healthcheck
            port: 8000
            {{- toYaml $.Values.fs_file_manager.livenessProbe | nindent 10 }}
        {{- end }}
        {{- if $.Values.fs_file_manager.readinessProbe }}
        readinessProbe:
          httpGet:
            path: /healthcheck
            port: 8000
            {{- toYaml $.Values.fs_file_manager.readinessProbe | nindent 10 }}
        {{- end }}
{{- end }}

