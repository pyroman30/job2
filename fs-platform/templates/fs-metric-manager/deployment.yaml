{{ if or (not (hasKey $.Values.fs_metric_manager "enabled")) (eq (coalesce $.Values.fs_metric_manager.enabled false) true) }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fs-metric-manager
  namespace: {{ $.Values.settings.namespace }}
  labels:
    app: fs-metric-manager
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: fs-metric-manager
  template:
    metadata:
      annotations:
      {{- if $.Values.fs_metric_manager.env }}
        checksum/configmap: {{ include (print $.Template.BasePath "/fs-metric-manager/configmap.yaml") . | sha256sum | quote }}
      {{- end }}
      
      labels:
        app: fs-metric-manager
    spec:
      containers:
      - name: {{ .Chart.Name }}
        image: {{ $.Values.fs_metric_manager.image.repository }}:{{ $.Values.fs_metric_manager.image.tag }}
        imagePullPolicy: {{ .Values.fs_metric_manager.image.pullPolicy }}
        ports:
        - containerPort: 80
        envFrom:
        {{- if $.Values.fs_metric_manager.env }}
        - configMapRef:
            name: fs-metric-manager-config
        {{- end }}
        
        - secretRef:
            name: fs-metric-manager-secret
        
        resources:
          requests:
            {{- toYaml $.Values.fs_metric_manager.requests | nindent 12 }}
          limits:
            {{- toYaml $.Values.fs_metric_manager.limits | nindent 12 }}
        {{- if $.Values.fs_metric_manager.livenessProbe }}
        livenessProbe:
          httpGet:
            path: /healthcheck
            port: 8000
            {{- toYaml $.Values.fs_metric_manager.livenessProbe | nindent 10 }}
        {{- end }}
        {{- if $.Values.fs_metric_manager.readinessProbe }}
        readinessProbe:
          httpGet:
            path: /healthcheck
            port: 8000
            {{- toYaml $.Values.fs_metric_manager.readinessProbe | nindent 10 }}
        {{- end }}
{{- end }}

