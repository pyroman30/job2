{{ if or (not (hasKey $.Values.fs_metric_manager "enabled")) (eq (coalesce $.Values.fs_metric_manager.enabled false) true) }}
kind: Service
apiVersion: v1
metadata:
  name: fs-metric-manager
  labels:
    app: fs-metric-manager
spec:
  type: {{ $.Values.fs_metric_manager.service.type | default "ClusterIP" | quote }}
  ports:
  - name: http
    protocol: TCP
    port: {{ $.Values.fs_metric_manager.service.port }}
    targetPort: {{ $.Values.fs_metric_manager.service.targetport }}
  selector:
    app: fs-metric-manager
{{- end }}