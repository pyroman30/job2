{{ if or (not (hasKey $.Values.fs_model_manager "enabled")) (eq (coalesce $.Values.fs_model_manager.enabled false) true) }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fs-model-manager
  namespace: {{ $.Values.settings.namespace }}
  labels:
    app: fs-model-manager
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: fs-model-manager
  template:
    metadata:
      annotations:
      {{- if $.Values.fs_model_manager.env }}
        checksum/configmap: {{ include (print $.Template.BasePath "/fs-model-manager/configmap.yaml") . | sha256sum | quote }}
      {{- end }}
      
      labels:
        app: fs-model-manager
    spec:
      containers:
      - name: {{ .Chart.Name }}
        image: {{ $.Values.fs_model_manager.image.repository }}:{{ $.Values.fs_model_manager.image.tag }}
        imagePullPolicy: {{ .Values.fs_model_manager.image.pullPolicy }}
        ports:
        - containerPort: 80
        envFrom:
        {{- if $.Values.fs_model_manager.env }}
        - configMapRef:
            name: fs-model-manager-config
        {{- end }}
        
        - secretRef:
            name: fs-model-manager-secret
        
        resources:
          requests:
            {{- toYaml $.Values.fs_model_manager.requests | nindent 12 }}
          limits:
            {{- toYaml $.Values.fs_model_manager.limits | nindent 12 }}
        {{- if $.Values.fs_model_manager.livenessProbe }}
        livenessProbe:
          httpGet:
            path: /healthcheck
            port: 8000
            {{- toYaml $.Values.fs_model_manager.livenessProbe | nindent 10 }}
        {{- end }}
        {{- if $.Values.fs_model_manager.readinessProbe }}
        readinessProbe:
          httpGet:
            path: /healthcheck
            port: 8000
            {{- toYaml $.Values.fs_model_manager.readinessProbe | nindent 10 }}
        {{- end }}
{{- end }}

