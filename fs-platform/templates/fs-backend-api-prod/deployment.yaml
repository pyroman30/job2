{{ if or (not (hasKey $.Values.fs_backend_api_prod "enabled")) (eq (coalesce $.Values.fs_backend_api_prod.enabled false) true) }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fs-backend-api-prod
  namespace: {{ $.Values.settings.namespace }}
  labels:
    app: fs-backend-api-prod
spec:
  replicas: {{ .Values.fs_backend_api_prod.replicaCount }}
  selector:
    matchLabels:
      app: fs-backend-api-prod
  template:
    metadata:
      annotations:
      {{- if $.Values.fs_backend_api_prod.env }}
        checksum/configmap: {{ include (print $.Template.BasePath "/fs-backend-api-prod/configmap.yaml") . | sha256sum | quote }}
      {{- end }}
      
      labels:
        app: fs-backend-api-prod
    spec:
      containers:
      - name: {{ .Chart.Name }}
        image: {{ $.Values.fs_backend_api_prod.image.repository }}:{{ $.Values.fs_backend_api_prod.image.tag }}
        imagePullPolicy: {{ .Values.fs_backend_api_prod.image.pullPolicy }}
        ports:
        - containerPort: 80
        envFrom:
        {{- if $.Values.fs_backend_api_prod.env }}
        - configMapRef:
            name: fs-backend-api-prod-config
        {{- end }}
        
        - secretRef:
            name: fs-backend-api-prod-secret
        
        resources:
          requests:
            {{- toYaml $.Values.fs_backend_api_prod.requests | nindent 12 }}
          limits:
            {{- toYaml $.Values.fs_backend_api_prod.limits | nindent 12 }}
        {{- if $.Values.fs_backend_api_prod.livenessProbe }}
        livenessProbe:
          httpGet:
            path: /healthcheck
            port: 8000
            {{- toYaml $.Values.fs_backend_api_prod.livenessProbe | nindent 10 }}
        {{- end }}
        {{- if $.Values.fs_backend_api_prod.readinessProbe }}
        readinessProbe:
          httpGet:
            path: /healthcheck
            port: 8000
            {{- toYaml $.Values.fs_backend_api_prod.readinessProbe | nindent 10 }}
        {{- end }}
{{- end }}

