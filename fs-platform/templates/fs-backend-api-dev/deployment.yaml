{{ if or (not (hasKey $.Values.fs_backend_api_dev "enabled")) (eq (coalesce $.Values.fs_backend_api_dev.enabled false) true) }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fs-backend-api-dev
  namespace: {{ $.Values.settings.namespace }}
  labels:
    app: fs-backend-api-dev
spec:
  replicas: {{ .Values.fs_backend_api_dev.replicaCount }}
  selector:
    matchLabels:
      app: fs-backend-api-dev
  template:
    metadata:
      annotations:
      {{- if $.Values.fs_backend_api_dev.env }}
        checksum/configmap: {{ include (print $.Template.BasePath "/fs-backend-api-dev/configmap.yaml") . | sha256sum | quote }}
      {{- end }}
      
      labels:
        app: fs-backend-api-dev
    spec:
      containers:
      - name: {{ .Chart.Name }}
        image: {{ $.Values.fs_backend_api_dev.image.repository }}:{{ $.Values.fs_backend_api_dev.image.tag }}
        imagePullPolicy: {{ .Values.fs_backend_api_dev.image.pullPolicy }}
        ports:
        - containerPort: 80
        envFrom:
        {{- if $.Values.fs_backend_api_dev.env }}
        - configMapRef:
            name: fs-backend-api-dev-config
        {{- end }}
        - secretRef:
            name: fs-backend-api-dev-secret
        resources:
          requests:
            {{- toYaml $.Values.fs_backend_api_dev.requests | nindent 12 }}
          limits:
            {{- toYaml $.Values.fs_backend_api_dev.limits | nindent 12 }}
        {{- if $.Values.fs_backend_api_dev.livenessProbe }}
        livenessProbe:
          httpGet:
            path: /healthcheck
            port: 8000
            {{- toYaml $.Values.fs_backend_api_dev.livenessProbe | nindent 10 }}
        {{- end }}
        {{- if $.Values.fs_backend_api_dev.readinessProbe }}
        readinessProbe:
          httpGet:
            path: /healthcheck
            port: 8000
            {{- toYaml $.Values.fs_backend_api_dev.readinessProbe | nindent 10 }}
        {{- end }}
{{- end }}

