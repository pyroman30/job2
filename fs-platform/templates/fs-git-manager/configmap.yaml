{{ if or (not (hasKey $.Values.fs_git_manager "enabled")) (eq (coalesce $.Values.fs_git_manager.enabled false) true) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: fs-git-manager-config
data:
{{- if $.Values.fs_git_manager.env }}
{{- toYaml $.Values.fs_git_manager.env | nindent 2 }}
{{- end }}
{{- end }}

